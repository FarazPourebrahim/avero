---
title: PriceCard
description: The base price panel — a caption over a large primary amount and its currency.
---

**Reference:** service detail sidebar (R-05) · **Tier:** A · **Block:** B-23 · [View source](https://github.com/FarazPourebrahim/avero/tree/dev/packages/react/src/blocks/price-card)

```tsx
import { PriceCard } from "@avero/react";
```

<ComponentPreview name="price-card/base" />

## Usage

```tsx
<PriceCard amount={service.price} />

<PriceCard amount={service.price} label="قیمت هر ساعت مشاوره">
  <Button block>سفارش خدمت</Button>
</PriceCard>
```

`amount` is a number, not a string: it is formatted with the active locale's digits and grouping,
so the same value renders "۲۰٬۰۰۰٬۰۰۰" in Persian and "20,000,000" in English. The caption and
currency both come from the dictionary.

Anything passed as children appears under the price. The reference's own card has nothing there —
it is a price, and the contact panel below it carries the action.

## Anatomy

| Part    | Slot                           | Notes                               |
| ------- | ------------------------------ | ----------------------------------- |
| Root    | `data-slot="price-card"`       | A `Card` surface                    |
| Caption | `data-slot="price-card-label"` | The small bold line above the price |
| Price   | `data-slot="price-tag"`        | `PriceTag` in its `display` variant |

## API reference

`PriceCard` accepts every native `<div>` attribute plus:

<PropsTable file="blocks/price-card/PriceCard.tsx" name="PriceCardOwnProps" />

## Do and don't

- **Do** pass the raw number and let the locale format it.
- **Don't** put a second price in the same card; the panel exists to make one number unmissable.

## Related

[PriceTag](../components/price-tag) · [ProviderCard](./provider-card) · [ContactMethods](./contact-methods)
