---
title: CategoryLinks
description: The related categories section — a small titled header over a row of tag links.
---

[View source](https://github.com/FarazPourebrahim/avero/tree/dev/packages/react/src/blocks/category-links)

```tsx
import { CategoryLinks, Chip } from "@avero/react";
```

<ComponentPreview name="category-links/related" />

## Usage

```tsx
<CategoryLinks
  title="دسته‌بندی‌های مرتبط"
  description="موضوع‌ها و زمینه‌های مرتبط با این مقاله"
  icon={<FolderTreeIcon />}
>
  {categories.map((category) => (
    <Chip key={category.slug} asChild variant="tag">
      <a href={category.href}>{category.name}</a>
    </Chip>
  ))}
</CategoryLinks>
```

The links are `Chip`s in their `tag` variant, rendered `asChild` around an anchor, which keeps the
hover treatment on the whole chip rather than only the text.

## Anatomy

| Part  | Slot                               | Notes                      |
| ----- | ---------------------------------- | -------------------------- |
| Root  | `data-slot="category-links"`       | Brings its own top divider |
| Items | `data-slot="category-links-items"` | The wrapping link row      |

The block usually closes an article or a course description rather than sitting on its own card,
so it carries a divider instead of a surface.

## API reference

`CategoryLinks` accepts every native `<div>` attribute plus:

<PropsTable file="blocks/category-links/CategoryLinks.tsx" name="CategoryLinksOwnProps" />

## Do and don't

- **Do** link every chip somewhere — a category that goes nowhere is a label, not a link.
- **Don't** repeat the page's own tags here; these are categories, not keywords.

## Related

[Chip](../components/chip) · [ReactionBar](./reaction-bar) · [FilterPanel](./filter-panel)
