---
title: SuggestionItem
description: A suggestion row, such as a recommended course — title, tags and a match percentage.
---

[View source](https://github.com/FarazPourebrahim/avero/tree/dev/packages/react/src/blocks/suggestion-item)

```tsx
import { SuggestionItem } from "@avero/react";
```

<ComponentPreview name="suggestion-item/dashboard" />

## Anatomy

| Part  | Slot                                | Notes                                    |
| ----- | ----------------------------------- | ---------------------------------------- |
| Root  | `data-slot="suggestion-item"`       | Bordered row; indigo tint on hover       |
| Title | `data-slot="suggestion-item-title"` | Truncated; links when `href` is given    |
| Tags  | `data-slot="suggestion-item-tags"`  | `Chip variant="category" size="sm"`      |
| Score | `MatchScore`                        | Percentage over the `matchLabel` caption |

## Composition

Built from `Chip` and `MatchScore`. The score is `MatchScore`, which formats the percentage in the
active locale's digits and captions it from the `matchLabel` dictionary entry:

```tsx
<SuggestionItem title="مبانی تحلیل داده" match={8} tags={["آنلاین"]} />
```

Stack rows in a `space-y-3` list to build a suggestions card on a dashboard.

## Accessibility

- Passing `href` makes the **title** a link, so each row is reachable by keyboard without changing
  how it looks.
- The title truncates visually but keeps its full text in the DOM, so assistive technology reads the
  whole title.

## API reference

`SuggestionItem` accepts every native `<div>` attribute plus:

<PropsTable file="blocks/suggestion-item/SuggestionItem.tsx" name="SuggestionItemOwnProps" />

## Do and don't

- **Do** keep summaries to one line; the row truncates rather than wraps.
- **Don't** put a second link in the row — the title is the target.

## Related

[MatchScore](../components/match-score) · [OpportunityCard](./opportunity-card) · [Chip](../components/chip)
