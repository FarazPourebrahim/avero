---
title: Elevation
description: Box shadows, drop shadows and blur radii, from Tailwind's defaults to Avero's card, floating and modal shadows.
---

Avero's shadows are soft and wide rather than dark: cards separate from the page with a few percent
of alpha, and only dialogs and the lightbox cast a heavy shadow.

## Box shadows

Use them as `shadow-<name>`.

<ShadowScale kind="shadow" />

## Drop shadows

Drop shadows follow the shape of transparent artwork. Use them as `drop-shadow-<name>`.

<ShadowScale kind="drop-shadow" />

## Blur

Blur radii for glass surfaces and decorative glows, used as `blur-<name>` or `backdrop-blur-<name>`.

<BlurScale />
