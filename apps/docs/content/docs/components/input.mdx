---
title: Input
description: The single-line text field, in the reference's three field treatments.
---

**Reference:** listing filter panel (R-06) · **Tier:** A · [View source](https://github.com/FarazPourebrahim/avero/tree/dev/packages/react/src/components/input)

```tsx
import { Input } from "@avero/react";
```

<ComponentPreview name="input/variants" />

## Anatomy

| Part | Slot                | Notes                                       |
| ---- | ------------------- | ------------------------------------------- |
| Root | `data-slot="input"` | Native `<input>`; `type` defaults to `text` |

`filter` is the reference's own filter box. `soft` and `slate` mirror the two comment-box
treatments (R-01, R-05) so an input can sit beside a [Textarea](./textarea) in the same form.

## Right-to-left

Text and placeholder follow the document direction. Pass `dir="ltr"` on fields that hold
left-to-right values such as emails, phone numbers or URLs.

## Composition

- Every input needs a label: a real `<label htmlFor>`, or `aria-label` when the design has none.
- Set `aria-invalid` while a field fails validation; the border and focus ring turn red (V-08).

## Accessibility

- Renders a plain `<input>`, so browser autofill, form validation and password managers work.
- `disabled` prevents input and dims the field; it is not a substitute for read-only.

## API reference

`Input` accepts every native `<input>` attribute plus:

<PropsTable file="components/input/Input.tsx" name="InputOwnProps" />

## Theming

| Token                | Used by              |
| -------------------- | -------------------- |
| `--color-gray-300`   | `filter` border      |
| `--color-blue-500`   | `filter` focus ring  |
| `--color-indigo-500` | `soft`/`slate` focus |

## Do and don't

- **Do** keep one variant per form.
- **Don't** use placeholder text as the only label.

## Related

[Textarea](./textarea) · [Select](./select) · [FormActions](./form-actions)
