---
title: Textarea
description: The comment box, in a soft gray and a slate treatment.
---

[View source](https://github.com/FarazPourebrahim/avero/tree/dev/packages/react/src/components/textarea)

```tsx
import { Textarea } from "@avero/react";
```

<ComponentPreview name="textarea/primary" />

## Anatomy

| Part | Slot                   | Notes                                     |
| ---- | ---------------------- | ----------------------------------------- |
| Root | `data-slot="textarea"` | Native `<textarea>`; `rows` defaults to 4 |

## Examples

### Variants

`soft` is the gray comment box and `slate` the bordered one; both take the indigo focus ring.

<ComponentPreview name="textarea/variants" />

### Resizing

The box is fixed by default, so a form's rhythm holds. `resize="vertical"` lets a reader drag it
taller for a long answer.

<ComponentPreview name="textarea/resize" />

### States

<ComponentPreview name="textarea/states" />

## Right-to-left

The box, its placeholder and the resize handle all follow the document direction.

## Composition

- `soft` has a gray ground; `slate` is white with a slate border.
- Resizing is off by default; pass `resize="vertical"` when a long answer is expected.

## Accessibility

- Give it a label, and pair it with an error message referenced by `aria-describedby`.
- Set `aria-invalid` while validation fails; the border and focus ring turn red.

## API reference

`Textarea` accepts every native `<textarea>` attribute plus:

<PropsTable file="components/textarea/Textarea.tsx" name="TextareaOwnProps" />

## Theming

| Token                | Used by        |
| -------------------- | -------------- |
| `--color-gray-50`    | `soft` ground  |
| `--color-slate-200`  | `slate` border |
| `--color-indigo-500` | Focus ring     |

## Do and don't

- **Do** state the moderation rule next to the submit button with [FormActions](./form-actions).
- **Don't** grow the box automatically while typing; a fixed height keeps the form from jumping.

## Related

[Input](./input) · [FormActions](./form-actions)
