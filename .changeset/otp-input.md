---
"@avero/react": minor
---

Add `OtpInput`, a one-time code field built on a single real input with `autocomplete="one-time-code"`. It accepts Persian and Arabic digits, cleans pasted codes, calls `onComplete` once the code is full, keeps its boxes left to right on RTL pages and shows the locale's digits.
