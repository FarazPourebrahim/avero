---
"@avero/react": minor
---

Add `Checkbox` (with an indeterminate state), `RadioGroup` with `RadioGroupItem`, and `Switch`, built on Radix. They submit with native forms, plug into `Field`, follow the `AveroProvider` direction, and their resting borders meet the 3:1 contrast required for form controls.
