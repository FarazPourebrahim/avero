# Known Debts

Every known gap, shortcut or mismatch lives here until it is resolved. Each entry states its impact and the plan to resolve it.
Remove an entry in the same change that resolves it.

| ID    | Area         | Debt                                                                                                                                                | Impact                                                                                                                            | Resolution plan                                                                                                       |
| ----- | ------------ | --------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------- |
| KD-01 | Reference    | Tier B components (see `Avero-plan.md` §7) have no captured markup or open states.                                                                  | They cannot be built to parity yet.                                                                                               | Blocked on the Phase 0.B capture list (Appendix A).                                                                   |
| KD-03 | Toolchain    | Local Node is 22.16; jsdom 30 and size-limit 13 need Node ≥ 22.18/22.22, so they are pinned one major back.                                         | None functionally; slightly older test tooling.                                                                                   | Upgrade local Node to the latest 22.x or 24 LTS, then bump jsdom and size-limit.                                      |
| KD-04 | Toolchain    | TypeScript is pinned to 6.0.x because typescript-eslint doesn't support TypeScript 7 yet.                                                           | We can't use the native TypeScript 7 compiler.                                                                                    | Bump when typescript-eslint's peer range includes 7.x.                                                                |
| KD-05 | Icons        | The footer Instagram glyph (`InstagramIcon`) comes from an unidentified icon set in the reference; its licence is unverified.                       | Cannot confirm redistribution rights for one icon.                                                                                | Identify the source set, or replace it with a licensed glyph of identical shape, before release (Phase 12).           |
| KD-06 | Visual tests | Screenshot baselines exist only for the local platform (`__screenshots__/win32`, `apps/replica/baselines/win32`); CI (Linux) skips `@visual` tests. | Phase 10's parity gate can only be run locally, and the Storybook screenshots catch regressions on one platform.                  | Generate Linux baselines in the pinned Playwright Docker image, commit them and drop `--grep-invert @visual` from CI. |
| KD-07 | Editor       | The reference contains no rendered Tiptap markup, so E-01's content styles cannot be diffed against a captured editor state.                        | The §5.11 values are reproduced from the compiled CSS and covered by unit tests, but never compared against a real editor render. | Capture an editor state as part of the Appendix A set (C-04), then add the visual diff and tick the Phase 9 DoD line. |
